# CD into Sites directories from anywhere else
# setopt autocd
# cdpath=($HOME/Sites)

# Turn off annoying auto-correction
setopt no_correct_all

# Don't beep on errors
setopt no_beep

# Case-insensitive globbing (used in pathname expansion)
setopt no_case_glob

